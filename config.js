'use strict';

// [NOTE]: This file will hold the all the default configurations
// Which the user can modify when requiring

// The dictionary object that has all the config properties
var config = {
    colored: true,
    typeCasing: 'lower',
    theme: ':type :message'
};

// Export the above object which is holding the config properties
module.exports = config;